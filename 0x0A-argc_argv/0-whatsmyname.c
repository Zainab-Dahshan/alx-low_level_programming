#include "main.h"
#include <stdio.h>

/**
 * main - main entry point
 * @argc: number of arguments to be passsed
 * @argv: the string array
 * Return: 0 Success
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
